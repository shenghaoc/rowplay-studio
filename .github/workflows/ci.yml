name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # RowPlayCore: Pure Swift domain logic — test on Linux for cross-platform validation
  core-linux:
    name: RowPlayCore (Linux)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - name: Verify Swift version
        run: swift --version
      - name: Build RowPlayCore
        run: swift build --target RowPlayCore
      - name: Test RowPlayCore
        run: swift test --filter RowPlayCoreTests

  # Full stack: macOS-only targets (RowPlayPlatform + RowPlayStudio)
  macos:
    name: macOS (Full Stack)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7
      - name: Verify Swift version
        run: swift --version
      - name: Test
        run: swift test
      - name: Build
        run: swift build
